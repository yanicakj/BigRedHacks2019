import React, { Component } from 'react';

class ZipInputHandler extends Component {
    constructor(props) {
        super(props);
        this.handleInput = this.handleInput.bind(this);
    }
}

export default ZipInputHandler;